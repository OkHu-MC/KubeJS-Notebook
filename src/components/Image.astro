---
import { Image as AstroImage } from 'astro:assets';

// このコンポーネントは、呼び出し元から画像パスを受け取ります
const { src, alt } = Astro.props;
---
<AstroImage
  src={src}
  alt={alt}
  width={600}
  height={360}
  loading="lazy"
/>